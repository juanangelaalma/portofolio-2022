import React, { useState } from "react";
import { MenuAdmin } from "../../components";

const Dashboard = () => {
  return (
    <div>
      <MenuAdmin />
    </div>
  );
};

export default Dashboard;
